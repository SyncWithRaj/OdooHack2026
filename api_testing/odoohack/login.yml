info:
  name: login
  type: http

http:
  method: POST
  url: http://localhost:5000/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "rajr127655@gmail.com",
        "password": "Test@1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
